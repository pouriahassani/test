// Developer- Seonghoon Kang
// Intro- Reads a spelt number and writes a corresponding decimal number.
// Details- http://www.ioccc.org/2012/kang/hint.html
#include "util.h"
#include "print.h"
char *p = "fifty-two trillion sixty-seven bilion and six-hundred and seventy five million and sixty-one thousand and five-hundred sixty-two\n";
uint32_t n,u,b,m,x;
void my_main(){
  char **r;
  int e;
  while((n++)||(e=*p++)){
        r=(char **)(b%64-25);
        while(e<47&&b){
            n=19;
            while(n){
                u=1ll<<6177%n--*4;
                if(((n["1+DIY/.K430x9G(kC["]-42)&255)^b){
                }
                else{
                    if(n>15){
                    }
                    else{
                            if (n > 9) {
                                if(n==0xE){
                                    x += (m%u) * (0x10000);
                                }
                                if(n==0xB){
                                    x += (m%u) * (0x10);
                                }
                                if(n==0xA){
                                    x += ((m%u)/0x100)%0x10;
                                }
                                m += (m % u) * (~(-u));
                            } else if (~(long)r) {
                                m += n + (!(long)r * 16);
                            } else {
                                m += n * 16;
                            }
                            b = 0;
                    }
                }
            }
            b/=8;
        }
    b="ynwtsflrabg"[n%=11]-e?b:b*8+n;
  }
  printHex(x);
  printHex(m);
}